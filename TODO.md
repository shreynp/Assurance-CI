X create a cli such that this can be initialised from the github actions
X use claude code ci to generate appropriate tests. 
use hypothesis, pytest-bdd and playwright for tests, browser stack and test rails if needed
X look for alternative harness for test generation
Domain.md for the the CI agent
flaging if no coverage
X Enhance JIRA tickets to be more concise
rename scripts to script-assurance-ci

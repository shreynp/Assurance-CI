create a cli such that this can be initialised from the github actions
use claude code ci to generate appropriate tests. 
use hypothesis, pytest-bdd and playwright for tests, browser stack and test rails if needed
look for alternative harness for test generation
